<?php

declare(strict_types=1);

namespace OSM\Frontend\Templates;

class LayoutTypes
{
    // Default layout for authorized user
    public const TYPE_DEFAULT = 'default';

    // Default layout for unauthorized user
    public const TYPE_DEFAULT_OUTSIDE = 'default-outside';
}
